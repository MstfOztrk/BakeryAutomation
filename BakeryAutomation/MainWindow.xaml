<Window x:Class="BakeryAutomation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BakeryAutomation.ViewModels"
        mc:Ignorable="d"
        Title="Firin Otomasyon"
        Width="1480"
        Height="980"
        MinWidth="1320"
        MinHeight="860"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Resources/Images/app-icon.ico"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{DynamicResource SidebarBrush}" Padding="16">
            <DockPanel LastChildFill="True">
                <Border DockPanel.Dock="Top"
                        Background="#12FFFFFF"
                        CornerRadius="18"
                        Padding="14"
                        Margin="0,6,0,20">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="pack://application:,,,/Resources/Images/logo.png"
                                   Width="54"
                                   Height="54"
                                   Margin="0,0,12,0"
                                   VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="HighQuality" />
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="Firin" FontSize="22" FontWeight="SemiBold" Foreground="White" />
                                <TextBlock Text="Otomasyon" FontSize="14" Foreground="#FF9CA3AF" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <StackPanel>
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsDashboardSelected}" Command="{Binding ShowDashboardCommand}" Content="Dashboard" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsProductsSelected}" Command="{Binding ShowProductsCommand}" Content="Urunler" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsBranchesSelected}" Command="{Binding ShowBranchesCommand}" Content="Subeler / Cariler" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsShipmentsSelected}" Command="{Binding ShowShipmentsCommand}" Content="Sevkiyat" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsPaymentsSelected}" Command="{Binding ShowPaymentsCommand}" Content="Tahsilat" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsReturnsSelected}" Command="{Binding ShowReturnsCommand}" Content="Iadeler" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsReportsSelected}" Command="{Binding ShowReportsCommand}" Content="Raporlar" />
                    <ToggleButton Style="{StaticResource SidebarNavButton}" IsChecked="{Binding IsSettingsSelected}" Command="{Binding ShowSettingsCommand}" Content="Ayarlar" />
                </StackPanel>

            </DockPanel>
        </Border>

        <Grid Grid.Column="1" Margin="20">
            <ContentControl Content="{Binding Current}" />
        </Grid>
    </Grid>
</Window>
